services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: discord-channel-bot:local
    container_name: discord-channel-bot
    user: "0:0"
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data